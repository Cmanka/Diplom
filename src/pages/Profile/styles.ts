import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles((theme) => ({
  root: {
    marginTop: '50px',
  },
  img: {
    width: '100%',
  },
  content: {
    display: 'flex',
    height: '100%',
    backgroundColor: '#dadada2e',
  },
}));

export default useStyles;
